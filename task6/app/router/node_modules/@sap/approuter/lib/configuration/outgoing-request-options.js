'use strict';

const { CLS, XSUAA, IDENTITY, HTML5_APPS_REPO, DESTINATION } = require('../utils/vcap-utils');

const DEFAULT_OUTGOING_REQUEST_OPTIONS = {
  [CLS]: { timeout: 0},
  [XSUAA]: {timeout: 10000},
  [IDENTITY]: {timeout: 30000},
  [HTML5_APPS_REPO]: {timeout: 0},
  [DESTINATION]: {timeout: 0}
};

exports.DEFAULT_OUTGOING_REQUEST_OPTIONS = DEFAULT_OUTGOING_REQUEST_OPTIONS;
